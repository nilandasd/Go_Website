import React from "react";
import styled from "styled-components";

const StyledPlayerInfo = styled.div`
  background: transparent;
`;

const PlayerInfo = () => {
  return <StyledPlayerInfo>Player Info</StyledPlayerInfo>;
};

export default PlayerInfo;